define(['app'], function (app) {
    app.registerController('PortalAnnounceCtrl', ['$scope' , function ($scope) {

        var init = function(){
            //$scope.$parent.active = 0;
        };

        init();
    }]);
});
