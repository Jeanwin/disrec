define(['app'], function (app) {
    app.registerController('CourseTVCtrl', ['$scope' , function ($scope) {

        var init = function(){
            //$scope.$parent.active = 0;
        };

        init();
    }]);
});
